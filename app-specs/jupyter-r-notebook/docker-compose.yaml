version: "3"
services:
  jupyter-r-notebook:
    image: "{{ helx_registry }}/helxplatform/jupyter/helx-r-notebook:latest"
    ports:
      - 8888:8888
    volumes:
        - pvc://projects-pvc:/projects
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 10G
        reservations:
          cpus: '2'
          memory: 10G
