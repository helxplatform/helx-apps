---
# Docker compose formatted system.
version: "3"
services:
  rstudio-server:
    image: "{{ helx_registry }}/helxplatform/rstudio-server:0.0.5"
    environment:
      - DISABLE_AUTH=true
    ports:
      - 8787:8787
    deploy:
      resources:
        limits:
          cpus: '4'
          memory: 16000M
        reservations:
          cpus: '1'
          memory: 1000M 
