---
# Docker compose formatted system.
version: "3"
services:
  rstudio-server:
    image: "containers.renci.org/helxplatform/rstudio-server:security-upgrades"
    # env:
    #   - RSTUDIO_LOG_LEVEL=debug
    #   - DELETE_DEFAULT_USER_HOME_IF_UNUSED=no
    ports:
      - 8787:8787
    volumes:
        - pvc://projects-pvc:/projects
    deploy:
        resources:
          limits:
            cpus: '2'
            memory: 6G
          reservations:
            cpus: '1'
            memory: 6G
