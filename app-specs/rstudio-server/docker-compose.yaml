---
# Docker compose formatted system.
version: "3"
services:
  rstudio-server:
    image: "containers.renci.org/helxplatform/rstudio-server:64a4af6-202502141219-dirty"
    # env:
    #   - DELETE_DEFAULT_USER_HOME_IF_UNUSED=no
    #   - RSTUDIO_LOG_LEVEL=debug
    ports:
      - 8787:8787
    volumes:
        - pvc://projects-pvc:/projects
    deploy:
        resources:
          limits:
            cpus: '2'
            memory: 10000M
          reservations:
            cpus: '2'
            memory: 10000M
